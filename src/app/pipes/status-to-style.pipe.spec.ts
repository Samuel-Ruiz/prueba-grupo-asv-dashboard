import { StatusToStylePipe } from './status-to-style.pipe';

describe('StatusToStylePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusToStylePipe();
    expect(pipe).toBeTruthy();
  });
});
